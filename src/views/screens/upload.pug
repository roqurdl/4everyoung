extends ../layout.pug

block contents
    form(method="POST")
        input(type="file" placeholder="Item image" accept="image/*" required)
        input(type="text" placeholder="Title of item" required)
        input(type="file" placeholder="File of information" required)
        input(type="text" placeholder="Description of item" required)
        input(type="text" placeholder="How much is it" required)
        input(type="submit" value="Upload")